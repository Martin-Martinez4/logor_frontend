
import React, { FC } from "react";

import "./Nav.css";

const BottomBar: FC = () => {

    return(
        <nav className="bottomBar">
            Bottom Nav
        </nav>
    );
}

export default BottomBar;
